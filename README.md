Created with Python 3.13\
Create VENV with python -m venv esphomeVENV\
activate VENV with ./esphomeVENV/Scripts/activate\
make sure pip is up to date with python -m pip install --upgrade pip\
get requirements with pip3 install -r requirements.txt
compile the components with esphome compile .\test_empty_components.yaml